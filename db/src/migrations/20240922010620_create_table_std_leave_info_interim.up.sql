CREATE TABLE IF NOT EXISTS std.leave_information_interim (
  id INT,
  user_id INT,
  emp_id INT,
  team_manager_id INT,
  designation_id INT,
  designation_name VARCHAR(500),
  first_name VARCHAR(500),
  middle_name VARCHAR(500),
  last_name VARCHAR(500),
  email VARCHAR(500),
  is_hr BOOLEAN,
  is_supervisor BOOLEAN,
  allocations JSONB,
  leave_issuer_id INT,
  issuer_first_name VARCHAR(500),
  issuer_middle_name VARCHAR(500),
  issuer_last_name VARCHAR(500),
  current_leave_issuer_id INT,
  current_leave_issuer_email VARCHAR(500),
  department_description VARCHAR(500),
  start_date TIMESTAMP,
  end_date TIMESTAMP,
  leave_days INT,
  reason TEXT,
  leave_status VARCHAR(500),
  status VARCHAR(500),
  response_remarks TEXT,
  leave_type_id INT,
  leave_type VARCHAR(500),
  default_days INT,
  transferable_days INT,
  is_consecutive BOOLEAN,
  fiscal_id INT,
  fiscal_start_date TIMESTAMP,
  fiscal_end_date TIMESTAMP,
  fiscal_is_current BOOLEAN,
  created_at TIMESTAMP,
  updated_at TIMESTAMP,
  is_automated INT,
  is_converted INT,
  total_count INT,
  ingested_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
